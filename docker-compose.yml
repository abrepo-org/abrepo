#prod first
version: '3.4'
services:

  pgbouncer:
    image: bitnami/pgbouncer
    depends_on:
      - pg1
    env_file:
      - .env.db
    environment:
      - POSTGRESQL_HOST=pg1  #required env variable for startup
      - POSTGRESQL_DATABASE=abrepo_www_prod
      - PGBOUNCER_DATABASE=abrepo_www_prod
      # NB: app connects to database 'postgres' - pgbouncer exposes a "view"
      # set in PGGBOUNER_DATABSE in app; in this case as 'prod'
    deploy:
      replicas: 1
      placement:
        constraints:
        - node.labels.db == true
        - node.labels.instance == db1

  #PRIMARY DB
  pg1:
    image: 'bitnami/postgresql:latest'
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 3s
      timeout: 10s
      retries: 3
    env_file:
      - .env.db
    volumes:
      - pg1data:/bitnami/postgresql
      - /mnt:/mnt   # pgbackrest backup directory (on instance)
      - /tmp:/tmpdb # exposed mount for scratch db/work output
      - "${REMOTE_RELEASE_PATH}/db/pgbackrest.conf:/etc/pgbackrest.conf"
      - "${REMOTE_RELEASE_PATH}/db/postgresql_custom.conf:/opt/bitnami/postgresql/conf/conf.d/postgresql_custom.conf"

    environment:
      - PGBACKREST_REPO1_PATH=/mnt/pgbackrest
      - PGBACKREST_LOG_PATH=/mnt/pgbackrest
      - PGBACKREST_REPO2_S3_BUCKET=abrepo-production-pg1-pgbackrest
    deploy:
      replicas: 1
      placement:
        constraints:
        - node.labels.db == true
        - node.labels.instance == db1

  web:
    image: '424385760710.dkr.ecr.us-east-2.amazonaws.com/vergeman/abrepo:latest'
    env_file:
      - .env
    command: bash -c "rm -f tmp/pids/server.pid && rake tmp:cache:clear && bundle exec rails s -p 8081 -b '0.0.0.0'"
    healthcheck:
      test: ["CMD-SHELL",
      "curl -o /dev/null -I -f -s -w %{http_code} http://127.0.0.1:8081/healthcheck || exit 1"]
      interval: 5s
      start_period: 15s
      timeout: 5s
      retries: 10

    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      placement:
        constraints:
          #- node.role == worker
          - node.labels.app == true
          #- node.role == manager

  nginx:
    image: '424385760710.dkr.ecr.us-east-2.amazonaws.com/vergeman/abnginx:latest'
    # healthcheck:
    #   test: ["CMD-SHELL",
    #   "curl -o /dev/null -I -f -s -w %{http_code} http://localhost:80/healthcheck || exit 1"]
    #   interval: 3s
    #   timeout: 10s
    #   retries: 3
    ports:
      - "80:80"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      placement:
        constraints:
          #- node.role == worker
          - node.labels.app == true
          #- node.role == manager

volumes:
  pg1data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/abrepo_pg1data
    #driver: rexray/dobs
    #driver_opts:
    #  size: 1
  portainer_data:
    driver: local
