apiVersion: v1
kind: Service
metadata:
  name: abrepo-rails-service
  namespace: abrepo
  labels:
    app: abrepo-rails-service
spec:
  selector:
    app: abrepo-rails
  ports:
    - name: "http"
      protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP
