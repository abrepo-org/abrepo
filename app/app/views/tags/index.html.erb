<% content_for :title, "Tag List and Variations | #{Rails.application.config.app_title}" %>
<% content_for :seo_meta_tags, seo_meta_tags(description: "Browse variations by tag with ABrepo") %>
<div class="header">
  <h1>Tags</h1>
</div>


<%= render partial: '/shared/subscribe_modal', locals: {
    noclose: false,
    is_active: false
} %>

<form id="tag-filter">
  <!-- JS -->
</form>

<hr />
<!-- Results -->
<%= render partial: 'tags' %>
