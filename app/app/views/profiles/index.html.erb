<% content_for :title, "ABRepo | Company Experimentation" %>
<div class="header">
    <h1>Companies</h1>


    <%= form_with(url: profiles_path,
                  skip_enforcing_utf8: true,
                  method: :get,
                  id: "profile-form",
                  class: "is-flex") do |form| %>

        <div class="field">
            <div id="search-control" class="control has-icons-left">

                <%= form.text_field :query,
                    class: "input is-small",
                        placeholder: "query",
                        value: params[:query]
                %>

                <span class="icon is-small is-left">
                    <i class="fas fa-search"></i>
                </span>
            </div>
        </div>

        <div class="field">
            <div class="control">
                <input class="ml-4 button is-small is-info"
                       type="submit" value="Submit">
            </div>
        </div>

    <% end %>

    <%= render partial: "results" %>

</div>
<hr />

<div class="profiles-data">

    <% @profiles.each do |profile| %>
        <%= render partial: "profile_card_row", locals: {profile: profile} %>
        <hr />
    <% end %>

</div>


<% content_for :sidebar do %>
    <%= render 'index_sidebar' %>
<% end %>
