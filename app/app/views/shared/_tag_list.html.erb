<% unless tag_list.empty? %>

    <% tag_list.each do |tag| %>

        <%= obfuscate_link_to obfuscated, search_path(tag_param.to_sym => "[#{tag.name}]"),
            classes do %>
            <%= tag.name %>
        <% end %>

    <% end %>

<% end %>
