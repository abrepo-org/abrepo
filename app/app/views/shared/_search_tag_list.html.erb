<% unless tag_list.empty? %>

    <% tag_list.each do |tag| %>
        <span class="<%= classes %>" >
            <%= link_to search_path(tag_param.to_sym => [tag]) do %>
                <%= tag %>
            <% end %>
        </span>
    <% end %>

<% end %>
