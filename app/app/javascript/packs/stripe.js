/*
 * stripe loaded as separate pack:
 * devise/registrations#edit (users/edit)
 * checkout/#ccount (checkout/account/:lookup_key)
 * stripe#subscriptions (checkout/subscriptions/:lookup_key)
 */
import "../js/stripe/init.js";
